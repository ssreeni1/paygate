mod config;
mod db;
mod metrics;
mod server;

// These modules will be implemented in worktree branches:
// mod admin;
// mod mpp;
// mod proxy;
// mod rate_limit;
// mod sponsor;
// mod verifier;

fn main() {
    println!("PayGate v0.1.0 — scaffold build");
    println!("Run worktree builds to add functionality.");
}
